#include "main.h"

/**
 * print_char - maini entry point
 * fuction to print char
 * @str: argument
 *
 * Return: to mai
 */


int print_char(char)
{
	va_list str;

	char = c;
	
	c = va_arg(str, int);
	_putchar(c);
	return (1);
}
